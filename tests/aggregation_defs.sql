-- AVG() -> returns average value in floating point value (2.342423)
--		ROUND(input_to_round, number_of_precision) -> specify precision after the decimal

-- COUNT() 
-- MAX()
-- MIN()
-- SUM -> returns the sum of all values

-- Aggregate functions ONLY work in SELECT or HAVING clause

--SELECT ROUND(AVG(amount), 2) FROM payment; -- round up to 2 decimals places

--SELECT MIN(replacement_cost) FROM film;