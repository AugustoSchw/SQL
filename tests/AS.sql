-- AS

-- is used to create an alias for a column or result
-- Ex:
--SELECT column AS new_column_name FROM table

-- IMPORTANT:
--     the alias is executed at the end of the query, so you can't use the alias inside a WHERE operator

